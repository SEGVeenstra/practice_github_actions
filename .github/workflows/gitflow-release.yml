name: GitFlow Release
on:
    pull_request:
        branches:
            - release/*
        types:
            - closed

jobs:
    if_merged:
        if: github.event.pull_request.merged
        runs-on: ubuntu-latest
        steps:
            - run: |
                echo The PR was merged!